image: maven:latest

stages:
  - build
  - test
  - dockerize

cache:
  paths:
    - service-categories/target/

build:
  stage: build
  script:
    - cd service-categories
    - mvn compile 

test:
  stage: test
  script:
    - cd service-categories
    - mvn test package

dockerize:
  stage: dockerize
  script:
    - cd service-categories
