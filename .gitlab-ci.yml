image: maven:latest

stages:
  - build
  - test
  - run

cache:
  paths:
    - .m2/repository/
    - target/

build:
  stage: build
  script:
    - cd service-categories
    - mvn compile

test:
  stage: test
  script:
    - mvn test

run:
  stage: run
  script:
    - mvn install
    - java -jar  target/service-categories-0.0.1-SNAPSHOT.jar
